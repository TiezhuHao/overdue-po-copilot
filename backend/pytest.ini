[pytest]
addopts = -ra
testpaths = tests
markers =
    integration: requires an explicit disposable PostgreSQL TEST_DATABASE_URL
    llm_integration: opt-in paid OpenAI smoke test; never part of the default suite
